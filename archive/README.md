# Archive

This folder keeps older standalone pages that are not part of the active public site structure.

- `brillbrains-single-page.html` is the previous one-file version of the BrillBrains website.
- The root `brillbrains.html` file now redirects to `index.html` so old links keep working.
